(()=>{

    // const numbers: (number | string | boolean)[] = [1,3,4,5,'6',7,8,9,10];
    // numbers.push(true);
    // numbers.push(11);
    // console.log(numbers);
    const numbers = [1,3,4,5,'6',7,8,9,10];

    const villians = ['Omega Rojo', 'Dormamu', 'Green Goblin'];

    villians.forEach( v => console.log(v.toUpperCase()));
    
})();